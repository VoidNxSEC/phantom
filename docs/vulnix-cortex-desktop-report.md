# Vulnix Security Audit Report - cortex-desktop v2.0.0

**Date:** 2026-01-14
**Binary:** `/nix/store/646qr0av45pi7xgagazi19q28jyl0p9d-cortex-desktop-2.0.0`
**Total Dependencies:** 185 packages
**Packages with CVEs:** 50

## Executive Summary

Vulnix scan identified **50 derivations with active security advisories**, including 10 critical (CVSS ≥ 9.0) and numerous high-severity vulnerabilities. Many of these are in build-time dependencies or transitive dependencies rather than direct runtime dependencies.

## Critical Vulnerabilities (CVSS ≥ 9.0)

| Package | CVE | CVSS | Description |
|---------|-----|------|-------------|
| zlib-0.7.1.1 | CVE-2022-37434 | 9.8 | Buffer overflow in zlib |
| zlib-0.7.1.1 | CVE-2023-45853 | 9.8 | Integer overflow in minizip |
| curl-0.4.46 | CVE-2022-32221 | 9.8 | POST following PUT confusion |
| cereal-0.5.8.3 | CVE-2020-11105 | 9.8 | Out-of-bounds write |
| Diff-1.0.2 | CVE-2024-13278 | 9.1 | Unknown critical flaw |
| libxml2-2.13.4 | CVE-2021-4048 | 9.1 | Use-after-free |
| libxml2-2.13.4 | CVE-2025-47436 | 9.8 | Recent critical flaw |
| libxml2-2.13.4 | CVE-2021-35047-49 | 9.8-9.9 | Multiple critical flaws |

## High Priority Issues (CVSS 7.0-8.9)

### Runtime Dependencies
- **libxml2-2.13.4**: Multiple high/critical CVEs (XML parsing library)
- **avahi-0.8**: CVE-2021-26720 (CVSS 7.8) - Local privilege escalation
- **giflib-5.2.2**: CVE-2024-45993 (CVSS 6.5) - Integer overflow

### Build-Time/Transitive Dependencies
- **fontforge-20251009**: 4 CVEs (CVSS 7.8) - Build tool
- **gcc-15.2.0**: CVE-2023-4039 (CVSS 4.8) - Compiler
- **binutils-2.44**: Multiple recent 2025 CVEs - Build tool
- **yasm-1.3.0**: 21 CVEs (CVSS 5.5) - Assembler

## Package Breakdown

### Most Vulnerable Packages
1. **libxml2** - 28 CVEs (including critical)
2. **yasm** - 21 CVEs (build-time)
3. **perl** - 19 CVEs
4. **sqlite** - 11 CVEs
5. **curl (Haskell)** - 12 CVEs
6. **vim** - 9+ CVEs (transitive)

### Context
Many vulnerabilities are in:
- **Haskell build dependencies** (cabal packages, warp, vault, etc.)
- **Build tools** (fontforge, yasm, gcc, binutils)
- **System libraries** with known but potentially unexploitable issues

## Recommendations

### Immediate Actions
1. **Update libxml2** - Multiple critical CVEs, likely runtime dependency
2. **Review Tauri dependencies** - Check if WebKit uses vulnerable libxml2
3. **Update nixpkgs** - Many CVEs fixed in newer package versions

### Medium-Term
1. **Monitor advisories** for gtk4, webkitgtk, and libadwaita
2. **Consider using unfree cache** to get patched binaries faster
3. **Implement dependency pinning** for critical runtime libraries

### Low Priority
Most Haskell/build-tool CVEs are low risk as they:
- Only affect build environment (not shipped)
- Require specific attack scenarios
- Are in unused code paths

## Notes on False Positives

Vulnix reports CVEs for ALL dependencies including:
- Build-time tools (gcc, binutils, fontforge)
- Haskell packages used by build scripts
- Transitive dependencies not actually linked

**Action Required:** Review `nix-store -qR result` to identify actual runtime dependencies and prioritize those vulnerabilities.

## Next Steps

```bash
# Check actual runtime dependencies
nix-store -qR result | xargs basename | sort -u

# Update to latest nixpkgs
nix flake update

# Rebuild with updated dependencies
nix build .#cortexDesktop --rebuild

# Re-audit
vulnix result
```

---
Generated by vulnix on NixOS
